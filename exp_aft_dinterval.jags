
model {
  for (i in 1:N) {
    isCens[i] ~ dinterval(t[i], time[i])
    t[i] ~ dexp(lambda[i])
    log(lambda[i]) <- -inprod(beta[], X[i,])  # AFT link
  }

  for (j in 1:K) {
    beta[j] ~ dnorm(0, 0.01)
  }
}
