
model {
  for (i in 1:N) {
    # Survival likelihood: censored or uncensored
    time[i] ~ dexp(lambda)
    loglik[i] <- status[i] * log(lambda) - lambda * time[i]
  }

  lambda ~ dgamma(0.001, 0.001)
}
